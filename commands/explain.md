---
description: Explain one entry point end to end, every claim cited — how does this work?
---

Run the `explain` skill for the entry point named in: $ARGUMENTS

If the arguments contain `--focus`, everything after it is a hint about where the
tracer should spend its file budget — pass it through. A hint steers attention; it
never substitutes for evidence.
