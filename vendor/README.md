`mermaid.min.js` — mermaid 11.17.1, MIT, fetched from
`https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.min.js`.

Vendored rather than linked: the generated wiki is one self-contained HTML file
that must render with no network. To update, refetch and bump the version above.
